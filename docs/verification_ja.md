# ロト・スコープ検証記録

2026年9月22日、Windows／Python 3.14、Flask 3.0.3、ephem 4.2.1、Node.js 24.19.0で検証。

- Python：50テスト成功。入力検証、固定候補、部分候補の範囲・一意性・順位・候補数変更、CSRF、有料占術アクセス拒否、本番でテスト権限が無効なこと、追加ページを確認。
- 生成マトリクス：120誕生日 × 3占術 × 5券種の1,800条件、7,200生成呼び出しに成功。
- オフラインDOM：15テスト成功。通信失敗、異常応答、429、CSRF、60秒タイムアウト、キャンセル、演出、再確認、BFCache、プレビューの時間切れ後再試行、個数選択の追従を確認。
- Edge実ブラウザー：45項目成功。320／390／768／1280pxで6ページの横はみ出し確認、3占術×5券種の生成・再確認、部分候補、JavaScript無効時の全5券種の規定数送信を確認。
- スクリーンショットでヒーロー、入力、少数結果、月額案内を確認。

無料構成とテスト専用の有料機能構成を使い分けました。実課金は未接続で検証対象ではありません。iOS Safari／Android実機、ネイティブアプリ、ストア審査、本番配信経路は未検証です。

初回はローカルNodeのバージョンとサンドボックス内の子プロセス制限によりDOMテストが起動できなかったため、対応するNode.js 24.19.0とローカル検証用の実行権限で再実行しました。最終実行は15件成功。

2026年9月23日：最終的なヒーロー配置・個数欄・件数表記を反映した状態で、Python 50件とEdge 45項目を再実行し成功。最終スクリーンショットも確認済み。

## v1.17.1（2026年9月23日）
一時無料開放と短いヒーローに更新。Python 51件、Edge 45項目に成功。3占術をテスト専用権限なしで生成・プレビューできること、無料開放を無効化した場合はロックが復帰することを検証。ヒーローの最終スクリーンショットを確認済み。前版の生成マトリクス・DOMの検証後、数字生成ロジックとJavaScriptには変更なし。

## v1.17.2（2026年9月23日）
スマートフォン向け文字サイズ・配置・コントラストを調整。Edgeで48項目成功。320〜1280pxの主要6画面、全15組み合わせ、JS無効時の規定数、320／390／430pxのロト7で詳細を展開した状態を検証。画像で文字と折り返しを確認。生成・アクセス制御ロジックに変更なし。実機のSafari等は未検証。

## v1.17.3（2026年9月23日）

- Python既存51件＋新規8件＝59件成功。
- 修正前のv1.17.2で記録した900条件（4誕生日×3生成日×3占術×全券種・全指定個数合計25）について、各10件の全フィールドをSHA-256で比較し一致。数字・採点・参考数字・理由・順序を維持。再生成の一致と1／3／5件が10件の先頭と一致することも確認。
- タロットは複数誕生日・日付で23〜43の全21数字がロト6の生成結果に現れること、候補プールにも高域が存在することを確認。ロト6・ロト7・ミニロトの範囲外出力なし。
- 影の循環、全22画像名、4役割、画像なし／一部ありの描画と配信、正午・スコアの文言を確認。
- 既存生成マトリクス1,800条件・7,200生成呼び出し、DOM15件成功。
- Edge52項目成功。既存48項目に加え、320／390／768／1280pxでタロット4枠の画像デコードと横はみ出しを確認。画像を目視確認。
- 初回の画像テストはWindowsの一時フォルダー権限制限で失敗したため、許可されたローカル実行で再検証して成功。候補プールは全43個の列挙ではないため、テストでは生成結果の高域全21数字と、候補プールの高域の存在を確認する設計とした。

占星術の式を1始まりへ変更する試算は300条件中300条件で数字出力が変化。本体の式は変更していない。詳しくは `REVISION_1_17_3_JA.md`。

## v1.17.4（2026年9月24日）

- 大アルカナ22枚を内蔵image_genで個別生成し、static/imgの既存対応名に配置。768×1152px WebP、22ファイルのハッシュはすべて異なる。
- 全22枚をブラウザーでデコードし、一覧で視線の方向・共通画風を目視確認。生成原稿とWebPの縦横比は2:3。
- Python59件成功。900条件の固定結果比較・再生成一致・1/3/5/10候補の先頭一致、画像欠落時フォールバック、セキュリティを含む。
- Edge52項目成功。320/390/768/1280pxの画面と画像読み込みを検証し、スマホの4カード表示を目視確認。実機Safariは未検証。
- 数字生成・採点・アクセス制御・CSS・JavaScriptは変更なし。変更は画像22枚、画像一覧・生成記録・説明文書とバージョン表記。

# v1.13.2 — Home column separation

トップ画面の機能・表示内容・生成ロジックは維持したまま、フォーム全体を覆っていた一枚のパネルを分割しました。

独立した表示ブロック:
- ヘッダー
- ヒーロー
- ロトを選ぶ
- 誕生日を選ぶ
- 受け取る口数 / サブ数字
- 星読みの数字を生成するボタン

各ブロック間に12px（560px以上では14px）の余白を設け、背景の星座画像が見えるようにしています。
ロジック、入力名、JS用data属性、生成先は変更していません。

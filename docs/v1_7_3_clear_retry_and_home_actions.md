# v1.7.3 結果画面の操作ボタンを明確化

## 変更内容

- 「同じ条件でもう一度」を主ボタンとして大きく表示。
- ボタンを押すと、同じ券種・誕生日・口数・合言葉で再生成。
- 再生成時も選択券種専用の星の儀式を表示。
- 「最初から選び直す」を副ボタンとして追加。
- 副ボタンは入力値を引き継がず、トップ画面へ直接戻る。
- 二つのボタンに説明文とアイコンを付け、役割を区別。

合言葉を設定している場合、「同じ条件でもう一度」は同じ数字を再現します。

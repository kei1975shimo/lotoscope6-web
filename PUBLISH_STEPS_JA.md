# ロトナンバーズ・スコープ v1.7.9 公開手順（GitHub + Render）

このZIPを解凍し、フォルダ内のファイル一式をGitHubへ置くと、Renderで公開できます。

## 1. GitHubへ登録

1. GitHubで新しいリポジトリを作成します。
2. このZIPを解凍します。
3. 解凍したフォルダの**中身すべて**を、リポジトリ直下へアップロードします。
4. GitHubの最上位に `app.py`、`render.yaml`、`requirements.txt` が見えていれば正しい配置です。

既存の `lotoscope6-web` を更新する場合は、新しいリポジトリを作らず、現在のリポジトリへ上書きしてください。

## 2. Renderで公開

1. Renderへログインします。
2. 新規公開の場合は「New」→「Blueprint」を選び、GitHubリポジトリを接続します。
3. Renderが `render.yaml` を読み取り、自動的にビルドと公開を行います。
4. デプロイが `Live` になったら公開URLを開きます。

`render.yaml`では`SECRET_KEY`をRender側で自動生成する設定になっています。

## 3. 公開後の確認

- 名称が「ロトナンバーズ・スコープ」になっている
- ミニロト、ロト6、ロト7、ナンバーズ3、ナンバーズ4が表示される
- 券種ごとに儀式名と紋章が表示される
- 選ぶ券種によって生成中の演出が変わる
- 生年月日を選ぶと太陽星座が表示される
- 各券種で指定した個数・桁数の数字が生成される
- ナンバーズで先頭の0が消えない
- 結果画面でも券種別の紋章と数字出現演出が表示される
- 答え合わせの項目が表示されない
- `/health` を開くと `OK` と表示される

## 答え合わせについて

答え合わせ機能は現在公開していません。

## 注意

このツールは数字選びを楽しむための星読みツールです。当せんを保証するものではありません。購入条件と正式な当せん結果は宝くじ公式情報で確認してください。

# LOTOSCOPE 6 公開用Flask版 v1.5.6

ロトスコープ6の公開用Webアプリです。ロト6の数字選びを補助し、生成した買い目を抽せん結果と照合できます。


## v1.5.6: iPhoneの生年月日表示を修正

- 10月・11月・12月や10日以降を選んだ際、左の1桁しか見えない問題を修正
- `年・月・日`を重ね表示せず、選択肢そのものに単位を含める方式へ変更
- iPhone／Android／Windowsで崩れにくい中央揃えと余白へ調整
- 公開画面のキャッシュ識別子を `v1.5.6-public` に更新

## v1.5.5: 星読みモード生成エラーの修正

- `Unknown mode_id: astrology` により数字を生成できない問題を修正
- 公開先に古い `config/mode_rules.json` が残っていても、アプリ側の安全な既定値で星読みモードを復旧
- `config/mode_rules.json` の設定値が存在する場合は、その内容を優先して統合
- 公開画面のキャッシュ識別子を `v1.5.5-public` に更新

## v1.5.4: 星の案内人による語りかけへ文章を刷新

- 入力画面を、人気占い師が寄り添って案内するような柔らかな文章へ変更
- 生成ボタンや占断中の文言を、星からの導きを受け取る体験として統一
- 各生成モードの名称と説明を、機械的な表現から占いらしい表現へ変更
- 結果画面を「今回もっとも調和した組み合わせ」「五つの導きから届いた数字」などへ変更
- 買い目の説明文も、数値的な根拠を保ちながら穏やかな占い師口調へ変更
- 答え合わせ画面も、明確さを保ちつつ柔らかな案内へ調整

## v1.5.3: スマホの星座表示レイアウト修正

- `SEALED`表示を右上へ独立配置し、星座カードの横幅を確保
- 星座マーク・日本語名・英語名が1文字ずつ縦に折り返される問題を修正
- 359px以下の狭いスマホ幅でも、星座名を単語単位で読みやすく表示

## v1.5.2: 生年月日選択リストの視認性修正

- Windows版Edgeなどで、生年月日の候補一覧が白背景・白文字になり読めない問題を修正
- 候補一覧を濃紺背景・白文字、選択中を紫紺背景・淡い金文字に統一

## v1.5.0: 占いサイト型の生年月日入力と星読み演出

- 生年月日を西暦・月・日の3つのセレクターで選択する占いサイト型フォームへ変更
- 選択した日付を「星図への刻印」として確認表示
- 生成ボタン後に約4.4秒の星読み演出を追加
- 星図・十二星座・天体軌道・六つの光が段階的に変化
- 結果画面では主要な数字が一つずつ現れる演出を追加


## 方針

- Flask + Gunicornで公開する前提
- 生成履歴・HTML・CSVをサーバーに保存しない
- ユーザーに任意ファイルパスを入力させない
- 1回の生成口数は各モード1〜10口（標準の全モード比較は星読みを含む最大50口、星読みOFF時は最大40口）
- 結果照合は、署名付き・期限付きの一時データを使う
- POSTフォームはCSRFトークンで保護する
- 簡易レート制限で連続送信を抑制する

## 星読みラッキーナンバー機能

西暦の生年月日を入力すると、生年月日と生成日の天体位置を組み合わせた「星読みラッキーナンバー」を作成できます。

- 入力形式: 西暦・月・日を分けた3つの選択欄
- 対象天体: 太陽、月、水星、金星、火星、木星、土星
- 計算時刻: 生年月日・生成日ともに正午12:00（日本時間）
- 計算座標: 地球から見た黄道上の位置（黄経）
- 星読みは初期状態でON: 専用の「星読みラッキー型」を含む5種類を標準比較
- 生年月日: サーバーへ永続保存しない

出生時刻と出生地を入力しない簡易版のため、アセンダントやハウスは計算しません。数字への変換方法は `src/astrology_numbers.py` に実装しています。星読みは数字選びを楽しむための独自方式であり、当せん確率を示すものではありません。

天体位置の計算には `ephem`（PyEphem）を使用します。外部APIへの送信や、実行時の天体データダウンロードは行いません。

## ローカル起動

```bash
pip install -r requirements.txt
python app.py
```

ブラウザで以下を開きます。

```text
http://127.0.0.1:8786/
```

## Renderでの公開

詳しい手順は `PUBLISH_STEPS_JA.md` を参照してください。`render.yaml` を使うBlueprint公開に対応しています。

### 手動設定する場合


Build Command:

```bash
pip install -r requirements.txt
```

Start Command:

```bash
gunicorn app:app
```

## 必須の環境変数

本番公開時は必ず設定してください。

```text
APP_ENV=production
SECRET_KEY=十分に長いランダム文字列
```

`APP_ENV=production` の状態で `SECRET_KEY` が未設定の場合は、危険なデフォルトキーで起動しないようにアプリが停止します。

## 任意の環境変数

```text
TICKET_TOKEN_MAX_AGE_SECONDS=86400
RATE_LIMIT_PER_MINUTE=30
```

- `TICKET_TOKEN_MAX_AGE_SECONDS`: 生成した買い目照合トークンの有効期限。標準は24時間です。
- `RATE_LIMIT_PER_MINUTE`: IPごとのPOST操作上限。標準は1分30回です。0以下にすると無効化します。

## データ更新

同梱データは `data/raw/loto6_results.csv` を参照しています。抽せん結果を追加・更新したあとは、以下で数字別スコアを再作成してください。

```bash
python src/analyze_numbers.py --input data/raw/loto6_results.csv --stats-output data/processed/number_stats.csv
```

アプリ画面には、過去データが14日以上古い場合に注意表示が出ます。購入・当せん確認は必ず公式情報をご確認ください。

## 注意

このツールは数字選びを補助するものです。当せんを保証するものではありません。過去の出現傾向は将来の抽せん結果を予測するものではありません。当せん結果は宝くじ公式情報をご確認ください。






## v1.4.2

- 星読みを初期状態でONに変更
- 標準の全モード比較を、星読みラッキー型を含む5種類に変更
- 星読みをOFFにした場合は従来どおり4種類で生成可能
- 誕生星・今日の天体・数字の記憶を重ねる神秘的な文言と演出へ刷新
- 生成中の表示を「誕生の星図」「今日の天体」「六つの数字」の三段階演出へ変更
- 入力条件へ戻った際も星読みON/OFFの状態を保持

## v1.4.1

- 「好きな数字」「避けたい数字」の入力欄を削除
- 「好きな数字重視型」を削除
- 全モード比較を通常4種類、星読み使用時5種類へ変更
- 入力数字の評価を総合スコアから削除し、重みを再配分
- 星読みなし: 過去データ45%／数字構成35%／32〜43配分20%
- 星読みあり: 過去データ35%／数字構成25%／32〜43配分15%／星読み25%

## v1.4.0

- 西暦の生年月日入力を追加
- 生年月日と生成日の7天体から、星読みラッキーナンバー6個と候補プールを生成
- 「星読みラッキー型」を新設
- 星読みON時の全モード比較を6種類へ拡張
- 結果画面へ太陽星座、月星座、天体位置、主要アスペクトを表示
- 星読み使用時は総合スコアへ「星読み数字との適合」20%を追加
- 星読み数字を✦マークと専用外枠で表示
- PyEphem 4.2.1を依存パッケージへ追加
- 星読み関連の自動テスト4件を追加（合計12件）

詳細は `docs/v1_4_0_astrology_number_engine.md` を参照してください。

## v1.3.0

- ホロスコープを意識した多重円スコープをヒーローへ復活
- 12星座、月相、星座線、天球儀のゆっくりした回転を追加
- 深い藍・紫・金を基調に、フォーチュン寄りの神秘的な世界観へ再設計
- 本文17px基準と大きな入力欄・ボタンは維持し、見た目と読みやすさを両立
- 背景、カード、ナビゲーション、ローダーまで占星術トーンへ統一

## v1.2.0

- スマートフォンを基準に、画面全体のデザインを再構築
- 本文17px、補足14px、ボタン17pxを基準に文字サイズを拡大
- 入力欄・数字入力・ボタン・ナビゲーションのタップ領域を拡大
- 3分割だった概要表示を狭い画面では2列＋全幅に変更
- 生成方法カードを大きくし、選択状態をラジオ表示で明確化
- 結果画面の買い目カードをスマホ幅では縦配置にし、数字の折返しを改善
- 結果・照合画面の操作ボタンを画面下で追従するモバイル操作バーへ変更
- 背景、カード、数字ボールを「夜空・艶・ゴールド・ネオン」の方向で統一
- トップ画面の巨大な天体リングを、買い目が一目で分かるチケット表示へ変更
- 360px幅、390px幅、600px幅で横スクロールが出ないことを確認

詳細は `docs/v1_2_0_mobile_first_ui.md` を参照してください。

## v1.1.0

- 公開画面の用語・説明・ボタン文言を全面的に整理
- 「全モード比較」の口数が各モードあたりであることと、合計予定口数を明示
- 「未出現数字重視型」を「出現間隔重視型」、「高配当意識型」を「32〜43重視型」へ変更
- 「バランス」を「総合スコア」へ改め、4項目の内訳と重みを表示
- 総合スコアが当せん確率ではないことを明記
- 照合方法を「抽せん回を指定」「抽せん数字を直接入力」の明確な二択へ変更
- 好きな数字・避けたい数字・本数字の重複、範囲外、ボーナス数字との重複を検証
- 入力条件を保った再編集と、照合条件を保った再照合に対応
- 全モード比較時に、異なるモード間でも同じ6数字の重複生成を防止
- 再現用キーワードの乱数処理をリクエスト単位に分離し、同時利用時の干渉を防止
- ローダーの強制待機を3秒から0.7秒へ短縮
- アコーディオンの開閉表示、最初に開く一致グループ、モバイル見出し折返しを改善
- CSP、Permissions-Policy、HTMLのno-storeなど公開向けヘッダーを追加

詳細は `docs/v1_1_0_public_review.md` を参照してください。

## 公開用パッケージでの追加対応

- Renderの現行Blueprint仕様に合わせて `runtime: python` を使用
- Pythonバージョンを `.python-version` で固定
- `/health` をRenderのヘルスチェック対象に設定
- 本番環境でセッションCookieに `Secure` / `HttpOnly` / `SameSite=Lax` を設定
- `__pycache__`、`*.pyc`、未使用の重複テンプレートを除外

## v1.0.13

- 本番環境で `SECRET_KEY` 未設定の場合は起動停止
- 生成結果トークンを期限付き署名へ変更
- POSTフォームにCSRF対策を追加
- 簡易レート制限を追加
- セキュリティヘッダーを追加
- データ鮮度の注意表示を追加
- ZIP配布物から `__pycache__` / `*.pyc` を除外


## v1.0.14
- スマホアプリ向けにUIを再調整
- 上部アプリバー、セクションナビ、固定操作ボタンを追加
- 入力欄・モードカード・照合画面をタップしやすいサイズに調整


## v1.0.15
- iPhoneアプリらしい明るいUIに再調整
- 白カード / 青ボタン / すりガラス風バー / iOS風配色に変更
- 入力・結果・照合画面をネイティブアプリ寄りの見た目に最適化

## v1.0.16
- 占星術ロータスUIを唯一の正式デザインとして再構築(重ね上書きのCSSを整理統合)
- ボタンの高さ・余白がバラついてずれて見えていた問題を修正
- スクロール時に固定ナビ同士が重なっていた問題を修正
- タップ時の縮小フィードバックなど、スマホアプリらしいモーションを追加
- 詳細は `v1_0_16_lotus_rebuild.md` を参照

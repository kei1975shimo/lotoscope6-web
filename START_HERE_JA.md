# ロト・スコープ v1.18.0 公開用ファイル

Flaskアプリの動作・配信に必要なファイルだけを収録しています。個別WebP画像50枚と共通画像3枚、画面・プライバシー等のページ、設定、依存関係、起動・配信設定を含みます。

## 起動

依存関係：`python -m pip install -r requirements.txt`

ローカル確認：`python app.py`

Linux本番：`gunicorn --workers 1 --threads 4 app:app`

本番ではAPP_ENV=production、十分に長いランダムなSECRET_KEYを環境変数に設定してください。Renderの設定はrender.yamlに含まれます。既存環境の環境変数も維持してください。

テスト、制作原稿、画像生成プロンプト、ギャラリー、開発用Node依存関係、キャッシュは収録していません。軽量画像は元PNGから書き出し、切り抜き・左右反転はしていません。

月額案内など既存ページは保持しています。ストア課金やネイティブアプリは含まれず、既存の一時無料開放設定は維持しています。

この版は占術ロジックを変更したため、同条件でも旧版と数字が変わります。新版内の再現性と候補数の先頭一致は維持します。採用方式と独自の数字対応は結果の詳細に表示します。

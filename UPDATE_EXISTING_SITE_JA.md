# 公開済みサイトを v1.1.0 へ更新する手順

すでにGitHubとRenderで公開している場合、Renderを作り直す必要はありません。GitHubのファイルを更新すると、Renderが自動で再デプロイします。

## 1. ZIPを展開する

`lotoscope6_v1.1.0_public_ready.zip` を右クリックし、「すべて展開」を選びます。

## 2. GitHubへアップロードする

1. GitHubで `lotoscope6-web` リポジトリを開きます。
2. `Add file` → `Upload files` を選びます。
3. 展開したフォルダそのものではなく、フォルダの中にあるファイルとフォルダをすべてアップロードします。
4. 既存ファイルは同じ場所へ上書きされ、新しいファイルは追加されます。
5. `Commit changes` を押して確定します。

リポジトリの一番上に `app.py`、`render.yaml`、`requirements.txt` が並んでいれば配置は正しい状態です。

## 3. Renderの再デプロイを確認する

GitHubへのコミット後、Renderで自動的に次の表示へ進みます。

```text
Building → Deploying → Live
```

自動で始まらない場合は、Renderの `Manual Deploy` → `Deploy latest commit` を押します。

## 4. 更新後に確認する項目

- トップ画面に「全モード比較」「合計予定口数」が表示される
- 初期設定が「各2口・合計10口」になっている
- 結果画面に「総合スコア」と4項目の内訳が表示される
- 照合方法が「抽せん回を指定」「抽せん数字を直接入力」の二択になっている
- 同じ数字を重複入力するとエラーになる
- 照合結果から「照合条件を変更」で戻れる

ブラウザに古い画面が残る場合は、`Ctrl + F5` で強制再読み込みしてください。
